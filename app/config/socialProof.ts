import { SocialProofConfig } from "../../lib/shared/components/src/components/SocialProof"

export const socialProofConfig: SocialProofConfig = {
  trustText: "Trusted by 10,000+ companies worldwide",
  companies: ["Microsoft", "Google", "Amazon", "Shopify", "Stripe", "Notion"]
}
